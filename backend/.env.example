// MongoDB Configuration
MONGODB_URI=mongodb+srv://arshdeep24:Arsh0987k@cluster0.ojmklpo.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

// JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_here_make_it_long_and_complex
JWT_EXPIRE=7d

// Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

// Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

// Email Configuration (for notifications and certificates)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=noreply@lms-platform.com

// File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

// AI/Chatbot Configuration (if using external AI service)
OPENAI_API_KEY=your_openai_api_key_here
CHATBOT_MODEL=gpt-3.5-turbo

# Instructions for setup:
# 1. Replace all placeholder values with your actual credentials
# 2. For MongoDB: Create a cluster on MongoDB Atlas and get the connection string
# 3. For JWT_SECRET: Generate a secure random string (at least 32 characters)
# 4. For Stripe: Get keys from your Stripe dashboard
# 5. For Email: Use Gmail app password or other SMTP service
# 6. Keep this file secure and never commit it to version control